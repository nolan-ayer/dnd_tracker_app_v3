import LoginForm from "./LoginForm";
import LoginNote from "./LoginNote";

const LoginMain = () => {
  return (
    <div>
      <LoginNote />
      <LoginForm />
    </div>
  );
};

export default LoginMain;
